struct block {
  std::string bytes;
};
  
class Disk {
 private:
  block *data;
  int allocationMethod;
 public:
  Disk(int);
  block read(int);
  void write(block, int, int);
  void deleteBlock(int);
};
